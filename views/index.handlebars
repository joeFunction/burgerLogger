<h1>Restaurant</h1>

<div class="row">
  <div class="col-6">
    <h2>Open Table area...</h2>

    <form class="create-form">

  <div class="form-group">
    <label for="ca">Table Number:</label>
    <input type="text" id="burger" name="name">
  </div>
  <button id="add">Add</button>
</form>

    <ul>
      {{#each burger}}
      {{#unless devoured}}
      <li>
        <h3>Table Number {{burger_name}}</h3>
        <h4>Food</h4> 
        <form class="create-form">
          <div class="form-group">
            <label for="ca">Food:</label>
            <input type="text" id="burger" name="name">
          </div>
        <button class="eat" data-id="{{id}}">Add</button>
        <button class="eat" data-id="{{id}}">Delete</button>

        <h4>Drinks</h4> 
        <form class="create-form">
          <div class="form-group">
            <label for="ca">Drinks:</label>
            <input type="text" id="burger" name="name">
          </div>
        <button class="eat" data-id="{{id}}">Add</button>
        <button class="eat" data-id="{{id}}">Delete</button>
       
        <button class="eat" data-id="{{id}}">Checkout</button>
      
      <form class="create-form">

  <div class="form-group">
    <label for="ca">Table Number:</label>
    <input type="text" id="burger" name="name">
  </div>
  <button id="add">Add</button>
</form>
      {{/unless}}
      {{/each}}
    </ul>
    </li>
  </div>


  <div class="col-6">
    <h2>Closed Table Area...</h2>
    <ul>
      {{#each burger}}
      {{#if devoured}}
      <li>
        {{burger_name}}
        <h3>Closed</h2>
        <ul>
          {{#each burger}}
          {{#if devoured}}
          <li>
            {{burger_name}}
          </li>
          {{/if}}
          {{/each}}
        </ul>
      </li>
      {{/if}}
      {{/each}}
    </ul>
  </div>
</div>


